
# Name : Robins Yadav
# Roll : 24BECSE47
# PS : Perform Clustering using K-Means

### Problem Statement that I am solving using K-Means Clustering :
“Organizing daily tasks automatically based on priority and their types.”

### My Daily tasks:
        studying
        gym
        assignments
        chilling
        urgent work

> Instead of manually sorting them, we’ll cluster tasks into:
        High priority
        Medium
        Low

### Idea Behind the Prototype
We’ll use K-Means to group tasks based on features like:
        Time required (minutes)
        Urgency (1–10)
        Efforts (1–10)

> Then K-Means will automatically create clusters.

### Prototype Workflow
Input daily tasks
Convert them into numerical features
Apply K-Means
Get clusters
Interpret clusters as priority levels

### Real-Life Value
This simple system can:
- Help you plan your day smarter
- Reduce decision fatigue
- Automatically prioritize work